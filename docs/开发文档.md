# MES 系统 SaaS 版 V2.1 - 开发文档

## 文档版本
- 版本号：V1.0
- 创建日期：2024
- 基于需求文档：MES 系统设计方案 Saas 版 V2.1

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术架构](#2-技术架构)
3. [模块拆分](#3-模块拆分)
4. [数据库设计](#4-数据库设计)
5. [API 接口规范](#5-api-接口规范)
6. [开发计划](#6-开发计划)

---

## 1. 系统概述

### 1.1 项目背景
本系统是一个面向制造业的 SaaS 化 MES（制造执行系统），旨在帮助中小制造企业实现数字化转型。系统采用云原生、全栈微服务架构，支持多租户隔离，可承载千级租户。

### 1.2 核心目标
- **多租户支持**：逻辑隔离与物理隔离相结合的混合式多租户数据模型
- **高可用性**：微服务架构确保局部故障不影响整体
- **极致用户体验**：针对不同角色设计专属交互终端
- **数据驱动决策**：从实时操作数据中提炼商业洞察

### 1.3 用户角色
| 角色 | 使用终端 | 主要功能 |
|------|----------|----------|
| 工人 | 微信小程序 | 扫码报工、查看任务、提交日报 |
| 班组长 | 小程序/PC | 批量报工、审核日报、进度监控 |
| 销售经理 | PC 管理后台 | 客户管理、报价、订单管理 |
| 生产主管 | PC/小程序 | 工艺配置、排产、进度监控 |
| 仓库管理员 | PC/PDA | 入库、出库、盘点 |
| 财务/老板 | PC 管理后台 | 成本分析、利润看板、决策支持 |
| 供应商/客户 | H5 页面（扫码） | 查看订单进度（只读） |

---

## 2. 技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                                │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐    │
│  │ 微信小程序   │  │ PC 管理后台   │  │ H5 分享页面      │    │
│  │ (工人端)     │  │ (Vue3+EP)    │  │ (客户/供应商)    │    │
│  └─────────────┘  └──────────────┘  └─────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      API 网关层                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Nginx / Kong + 身份认证 + 限流 + 路由               │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    微服务层                                  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐   │
│  │用户中心│ │销售管理│ │生产管理│ │仓储管理│ │财务管理│   │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘   │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐   │
│  │发货管理│ │发票管理│ │物流管理│ │外协管理│ │ BI 报表  │   │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据层                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  MySQL   │  │  Redis   │  │   OSS    │  │  DataWh  │   │
│  │  集群     │  │  集群     │  │  对象存储 │  │  数据仓库 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈选型

#### 后端技术栈
| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 开发语言 | Java 17 / Python 3.10+ | 微服务开发 |
| Web 框架 | Spring Boot 3.x / FastAPI | RESTful API |
| 服务发现 | Nacos / Consul | 服务注册与配置中心 |
| API 网关 | Nginx + Kong | 统一接入、鉴权、限流 |
| 数据库 | PostgreSQL 15.x 集群 | 核心业务数据存储 |
| 缓存 | Redis 7.x 集群 | 会话存储、热点数据缓存 |
| 消息队列 | RabbitMQ / Kafka | 异步解耦、事件驱动 |
| 对象存储 | 阿里云 OSS / MinIO | 文件、图片存储 |
| 数据仓库 | ClickHouse / Doris | BI 分析引擎 |

#### 前端技术栈
| 终端 | 技术选型 | 说明 |
|------|----------|------|
| PC 管理后台 | Vue 3.x + Element Plus | 响应式管理界面 |
| 微信小程序 | 原生小程序 / Taro | 工人操作终端 |
| H5 分享页 | Vue 3 + Vant | 客户进度查询 |
| 移动端看板 | 小程序 / H5 | 管理者移动查看 |

### 2.3 多租户架构设计

#### 租户隔离策略
系统采用混合隔离模式，支持两种部署方式：

1. **共享数据库 + Schema 隔离**（默认）
   - 所有租户共享数据库实例
   - 每个租户独立 Schema
   - 适用于中小型客户

2. **独立数据库隔离**
   - 每个租户独立数据库实例
   - 最高级别的数据安全
   - 适用于大型/高安全要求客户

#### 租户识别流程
```
请求 → API 网关 → 解析 Token → 提取 tenant_id → 路由到对应 Schema/DB
```

---

## 3. 模块拆分

### 3.1 模块总览

| 模块编号 | 模块名称 | 服务名 | 优先级 |
|----------|----------|--------|--------|
| M01 | 销售与订单管理 | sales-service | P0 |
| M02 | 生产进度管理 | production-service | P0 |
| M03 | 智能仓储管理 | warehouse-service | P0 |
| M04 | 发货与客户分享 | delivery-service | P1 |
| M05 | 财务管理 | finance-service | P1 |
| M06 | 发票管理 | invoice-service | P2 |
| M07 | 物流费用管理 | logistics-service | P2 |
| M08 | 外协管理 | subcontract-service | P2 |
| M09 | BI 报表中心 | bi-service | P1 |
| M10 | 用户与权限中心 | user-service | P0 |

### 3.2 详细模块设计

#### M01 - 销售与订单管理模块

**功能清单：**
- 客户与商机中心
- 快速报价与成本模拟
- 订单全生命周期管理
- 销售看板与预警

**核心实体：**
- Customer（客户）
- Quotation（报价单）
- SalesOrder（销售订单）
- Product（产品）

**API 端点：**
```
POST   /api/v1/customers          # 创建客户
GET    /api/v1/customers          # 客户列表
PUT    /api/v1/customers/{id}     # 更新客户
DELETE /api/v1/customers/{id}     # 删除客户

POST   /api/v1/quotations         # 创建报价
POST   /api/v1/quotations/{id}/convert-to-order  # 转订单
GET    /api/v1/quotations         # 报价列表

POST   /api/v1/orders             # 创建订单
GET    /api/v1/orders             # 订单列表
PUT    /api/v1/orders/{id}        # 更新订单
GET    /api/v1/orders/{id}/progress  # 订单进度

GET    /api/v1/dashboard/sales    # 销售看板
```

---

#### M02 - 生产进度管理模块

**功能清单：**
- 图形化工艺路线配置
- 多渠道进度实时采集（扫码报工）
- 自动化工人日报
- 可视化监控与预警
- 异常处理流程

**核心实体：**
- ProcessRoute（工艺路线）
- ProcessStep（工序）
- WorkOrder（工单）
- WorkReport（报工记录）
- DailyReport（日报）

**API 端点：**
```
POST   /api/v1/process-routes     # 创建工艺路线
GET    /api/v1/process-routes     # 工艺路线列表
PUT    /api/v1/process-routes/{id}# 更新工艺路线

POST   /api/v1/work-orders        # 创建工单
GET    /api/v1/work-orders        # 工单列表
PUT    /api/v1/work-orders/{id}/status  # 更新状态

POST   /api/v1/work-reports/scan  # 扫码报工开始
POST   /api/v1/work-reports/complete  # 报工完成
GET    /api/v1/work-reports       # 报工记录

POST   /api/v1/daily-reports      # 提交日报
GET    /api/v1/daily-reports/pending  # 待审核日报
PUT    /api/v1/daily-reports/{id}/approve  # 审核日报

GET    /api/v1/dashboard/production  # 生产看板
GET    /api/v1/gantt-chart        # 甘特图数据
```

---

#### M03 - 智能仓储管理模块

**功能清单：**
- 全域物料与仓位管理
- 全流程扫码作业（入库/出库/盘点）
- 智能预警体系（低库存/消耗加速/呆滞品）
- 常备零件专项管理

**核心实体：**
- Material（物料）
- Warehouse（仓库）
- Location（货位）
- Stock（库存）
- StockIn（入库单）
- StockOut（出库单）
- StockTake（盘点单）

**API 端点：**
```
POST   /api/v1/materials          # 创建物料
GET    /api/v1/materials          # 物料列表
PUT    /api/v1/materials/{id}     # 更新物料

POST   /api/v1/locations          # 创建货位
GET    /api/v1/locations          # 货位列表
GET    /api/v1/locations/{code}/materials  # 货位上的物料

POST   /api/v1/stock-in           # 入库
POST   /api/v1/stock-out          # 出库
POST   /api/v1/stock-take         # 盘点

GET    /api/v1/stock/warnings     # 库存预警
GET    /api/v1/stock/stagnant     # 呆滞品列表

GET    /api/v1/dashboard/warehouse  # 仓储看板
```

---

#### M04 - 发货与客户分享模块

**功能清单：**
- 发货单管理
- 物流跟踪
- 客户进度分享二维码生成
- H5 进度展示页
- 访问日志

**核心实体：**
- DeliveryOrder（发货单）
- ShipmentShare（分享配置）
- AccessLog（访问日志）

**API 端点：**
```
POST   /api/v1/deliveries         # 创建发货单
GET    /api/v1/deliveries         # 发货单列表
PUT    /api/v1/deliveries/{id}    # 更新发货

POST   /api/v1/shares/generate    # 生成分享二维码
GET    /api/v1/shares/{token}     # 获取分享数据（H5 用）
GET    /api/v1/shares/{id}/logs   # 访问日志

GET    /api/v1/logistics/track    # 物流跟踪
```

---

#### M05 - 财务管理模块

**功能清单：**
- 成本自动归集与核算
- 资金占用分析
- 利润分析看板
- 财务健康度仪表盘

**核心实体：**
- CostRecord（成本记录）
- ProfitAnalysis（利润分析）
- FundOccupancy（资金占用）

**API 端点：**
```
GET    /api/v1/costs/order/{orderId}  # 订单成本
GET    /api/v1/costs/product      # 产品成本分析
GET    /api/v1/profit/orders      # 订单毛利分析
GET    /api/v1/profit/products    # 产品利润率排行

GET    /api/v1/fund/inventory     # 库存资金占用
GET    /api/v1/fund/receivables   # 应收账款账龄

GET    /api/v1/dashboard/finance  # 财务健康度仪表盘
```

---

#### M06 - 发票管理模块

**功能清单：**
- 合同开票额度分解
- 灵活开票记录与核销
- 全景发票台账与看板

**核心实体：**
- InvoiceQuota（开票额度）
- InvoiceRecord（开票记录）

**API 端点：**
```
POST   /api/v1/invoice-quotas     # 分解开票额度
GET    /api/v1/invoice-quotas     # 额度列表

POST   /api/v1/invoice-records    # 创建开票记录
GET    /api/v1/invoice-records    # 开票记录列表

GET    /api/v1/invoice/ledger     # 发票台账
GET    /api/v1/invoice/dashboard  # 发票看板
```

---

#### M07 - 物流费用管理模块

**功能清单：**
- 运费单记录
- 成本自动归集与分摊
- 物流成本分析看板

**核心实体：**
- FreightOrder（运费单）
- FreightAllocation（运费分摊）

**API 端点：**
```
POST   /api/v1/freight-orders     # 创建运费单
GET    /api/v1/freight-orders     # 运费单列表

GET    /api/v1/freight/analysis/factory  # 工厂维度分析
GET    /api/v1/freight/analysis/customer # 客户维度分析
GET    /api/v1/freight/dashboard  # 物流看板
```

---

#### M08 - 外协管理模块

**功能清单：**
- 简易外协记录单
- 成本自动拆分与看板展示
- 外协成本汇总报告

**核心实体：**
- SubcontractRecord（外协记录）

**API 端点：**
```
POST   /api/v1/subcontracts       # 创建外协记录
GET    /api/v1/subcontracts       # 外协记录列表

GET    /api/v1/subcontracts/analysis  # 外协成本分析
GET    /api/v1/subcontracts/report    # 外协汇总报告
```

---

#### M09 - BI 报表中心模块

**功能清单：**
- 多角色数据驾驶舱
- 拖拽式自助报表分析
- 预置行业分析模型
- 智能钻取与溯源
- 报告自动生成与推送

**核心实体：**
- Dashboard（驾驶舱配置）
- ReportTemplate（报表模板）
- ReportTask（报表任务）

**API 端点：**
```
GET    /api/v1/bi/dashboard/{role}  # 角色驾驶舱
GET    /api/v1/bi/reports         # 自助报表
GET    /api/v1/bi/models          # 预置模型列表
GET    /api/v1/bi/drill-down      # 钻取查询

POST   /api/v1/bi/tasks           # 创建报表任务
GET    /api/v1/bi/tasks           # 任务列表
```

---

#### M10 - 用户与权限中心模块

**功能清单：**
- 多租户用户管理
- 角色与权限配置
- 登录认证（JWT）
- 操作日志

**核心实体：**
- Tenant（租户）
- User（用户）
- Role（角色）
- Permission（权限）
- OperationLog（操作日志）

**API 端点：**
```
POST   /api/v1/auth/login         # 登录
POST   /api/v1/auth/logout        # 登出
POST   /api/v1/auth/refresh       # 刷新 Token

POST   /api/v1/users              # 创建用户
GET    /api/v1/users              # 用户列表
PUT    /api/v1/users/{id}         # 更新用户

POST   /api/v1/roles              # 创建角色
GET    /api/v1/roles              # 角色列表
PUT    /api/v1/roles/{id}/permissions  # 配置权限

GET    /api/v1/logs               # 操作日志
```

---

## 4. 数据库设计

### 4.1 核心表结构

#### 租户表 (tenants)
```sql
CREATE TABLE tenants (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL COMMENT '租户名称',
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '租户编码',
    db_schema VARCHAR(50) COMMENT '独立 Schema 名称',
    db_instance VARCHAR(100) COMMENT '独立数据库实例（可选）',
    status SMALLINT DEFAULT 1 COMMENT '状态：1-正常，0-禁用',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 客户表 (customers)
```sql
CREATE TABLE customers (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    name VARCHAR(100) NOT NULL COMMENT '客户名称',
    contact_name VARCHAR(50) COMMENT '联系人',
    contact_phone VARCHAR(20) COMMENT '联系电话',
    contact_email VARCHAR(100) COMMENT '联系邮箱',
    address TEXT COMMENT '地址',
    tax_info JSONB COMMENT '开票资料',
    tags JSONB COMMENT '标签',
    notes TEXT COMMENT '备注',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_customers_tenant ON customers(tenant_id);
CREATE INDEX idx_customers_name ON customers(name);
```

#### 销售订单表 (sales_orders)
```sql
CREATE TABLE sales_orders (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    order_no VARCHAR(50) UNIQUE NOT NULL COMMENT '订单号',
    customer_id BIGINT NOT NULL COMMENT '客户 ID',
    quotation_id BIGINT COMMENT '关联报价单 ID',
    status SMALLINT DEFAULT 0 COMMENT '状态：0-待审核，1-待排产，2-生产中，3-已完工，4-已发货，5-已完成',
    total_amount DECIMAL(15,2) COMMENT '订单总金额',
    delivery_date DATE COMMENT '要求交期',
    attachments JSONB COMMENT '附件（图纸等）',
    special_requirements TEXT COMMENT '特殊要求',
    created_by BIGINT COMMENT '创建人',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_sales_orders_tenant ON sales_orders(tenant_id);
CREATE INDEX idx_sales_orders_customer ON sales_orders(customer_id);
CREATE INDEX idx_sales_orders_status ON sales_orders(status);
CREATE INDEX idx_sales_orders_delivery_date ON sales_orders(delivery_date);
```

#### 工艺路线表 (process_routes)
```sql
CREATE TABLE process_routes (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    name VARCHAR(100) NOT NULL COMMENT '工艺路线名称',
    product_type VARCHAR(100) COMMENT '适用产品类型',
    is_template BOOLEAN DEFAULT FALSE COMMENT '是否模板',
    steps JSONB NOT NULL COMMENT '工序步骤（有序数组）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_process_routes_tenant ON process_routes(tenant_id);
```

#### 工单表 (work_orders)
```sql
CREATE TABLE work_orders (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    order_no VARCHAR(50) UNIQUE NOT NULL COMMENT '工单号',
    sales_order_id BIGINT COMMENT '关联销售订单 ID',
    process_route_id BIGINT COMMENT '工艺路线 ID',
    current_step_index INT DEFAULT 0 COMMENT '当前工序索引',
    status SMALLINT DEFAULT 0 COMMENT '状态：0-未开始，1-进行中，2-已完成，3-已延误',
    plan_start_time TIMESTAMP COMMENT '计划开始时间',
    plan_end_time TIMESTAMP COMMENT '计划结束时间',
    actual_start_time TIMESTAMP COMMENT '实际开始时间',
    actual_end_time TIMESTAMP COMMENT '实际结束时间',
    quantity INT COMMENT '数量',
    completed_quantity INT DEFAULT 0 COMMENT '已完成数量',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_work_orders_tenant ON work_orders(tenant_id);
CREATE INDEX idx_work_orders_sales_order ON work_orders(sales_order_id);
CREATE INDEX idx_work_orders_status ON work_orders(status);
```

#### 报工记录表 (work_reports)
```sql
CREATE TABLE work_reports (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    work_order_id BIGINT NOT NULL COMMENT '工单 ID',
    step_index INT NOT NULL COMMENT '工序索引',
    worker_id BIGINT NOT NULL COMMENT '操作工 ID',
    start_time TIMESTAMP COMMENT '开始时间',
    end_time TIMESTAMP COMMENT '结束时间',
    quantity INT COMMENT '完成数量',
    qualified_quantity INT COMMENT '合格数量',
    unqualified_quantity INT COMMENT '不合格数量',
    unqualified_reason TEXT COMMENT '不合格原因',
    notes TEXT COMMENT '备注',
    status SMALLINT DEFAULT 1 COMMENT '状态：1-进行中，2-已完成',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_work_reports_tenant ON work_reports(tenant_id);
CREATE INDEX idx_work_reports_work_order ON work_reports(work_order_id);
CREATE INDEX idx_work_reports_worker ON work_reports(worker_id);
CREATE INDEX idx_work_reports_start_time ON work_reports(start_time);
```

#### 物料表 (materials)
```sql
CREATE TABLE materials (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    code VARCHAR(50) NOT NULL COMMENT '物料编码',
    name VARCHAR(100) NOT NULL COMMENT '物料名称',
    specification VARCHAR(100) COMMENT '规格型号',
    barcode VARCHAR(100) COMMENT '条码',
    category VARCHAR(50) COMMENT '分类',
    unit VARCHAR(20) COMMENT '单位',
    is_spare_part BOOLEAN DEFAULT FALSE COMMENT '是否常备零件',
    safety_stock INT DEFAULT 0 COMMENT '安全库存',
    purchase_price DECIMAL(12,4) COMMENT '采购单价',
    applicable_equipment JSONB COMMENT '适用设备（常备零件）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_materials_tenant ON materials(tenant_id);
CREATE INDEX idx_materials_code ON materials(code);
CREATE INDEX idx_materials_category ON materials(category);
```

#### 货位表 (locations)
```sql
CREATE TABLE locations (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    warehouse_id BIGINT COMMENT '仓库 ID',
    code VARCHAR(50) NOT NULL COMMENT '货位编码（如 A-01-02-3）',
    zone VARCHAR(20) COMMENT '区域',
    row VARCHAR(20) COMMENT '排',
    shelf VARCHAR(20) COMMENT '架',
    layer VARCHAR(20) COMMENT '层',
    qr_code VARCHAR(100) COMMENT '货位二维码',
    status SMALLINT DEFAULT 1 COMMENT '状态：1-可用，0-禁用',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_locations_tenant ON locations(tenant_id);
CREATE INDEX idx_locations_code ON locations(code);
CREATE UNIQUE INDEX uk_locations_tenant_code ON locations(tenant_id, code);
```

#### 库存表 (stocks)
```sql
CREATE TABLE stocks (
    id BIGSERIAL PRIMARY KEY,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    material_id BIGINT NOT NULL COMMENT '物料 ID',
    location_id BIGINT NOT NULL COMMENT '货位 ID',
    quantity INT NOT NULL DEFAULT 0 COMMENT '当前库存数量',
    locked_quantity INT DEFAULT 0 COMMENT '锁定数量',
    last_in_time TIMESTAMP COMMENT '最后入库时间',
    last_out_time TIMESTAMP COMMENT '最后出库时间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_stocks_tenant ON stocks(tenant_id);
CREATE INDEX idx_stocks_material ON stocks(material_id);
CREATE INDEX idx_stocks_location ON stocks(location_id);
CREATE UNIQUE INDEX uk_stocks_tenant_material_location ON stocks(tenant_id, material_id, location_id);
```

#### 发货单表 (delivery_orders)
```sql
CREATE TABLE delivery_orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    delivery_no VARCHAR(50) UNIQUE NOT NULL COMMENT '发货单号',
    sales_order_id BIGINT NOT NULL COMMENT '销售订单 ID',
    customer_id BIGINT NOT NULL COMMENT '客户 ID',
    status TINYINT DEFAULT 0 COMMENT '状态：0-待发货，1-部分发货，2-已发货',
    items JSON COMMENT '发货明细',
    logistics_company VARCHAR(100) COMMENT '物流公司',
    tracking_no VARCHAR(100) COMMENT '物流单号',
    ship_date DATETIME COMMENT '发货日期',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_tenant (tenant_id),
    INDEX idx_sales_order (sales_order_id)
);
```

#### 分享配置表 (shipment_shares)
```sql
CREATE TABLE shipment_shares (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    token VARCHAR(64) UNIQUE NOT NULL COMMENT '分享令牌',
    target_type VARCHAR(20) COMMENT '类型：order-订单，item-零件',
    target_id BIGINT NOT NULL COMMENT '目标 ID',
    scope JSON COMMENT '展示范围配置',
    expire_time DATETIME COMMENT '过期时间',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否有效',
    view_count INT DEFAULT 0 COMMENT '访问次数',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_token (token),
    INDEX idx_target (target_type, target_id)
);
```

#### 开票额度表 (invoice_quotas)
```sql
CREATE TABLE invoice_quotas (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    sales_order_id BIGINT NOT NULL COMMENT '销售订单 ID',
    category VARCHAR(50) NOT NULL COMMENT '类别（如设备款、加工费）',
    total_amount DECIMAL(15,2) NOT NULL COMMENT '额度总额',
    invoiced_amount DECIMAL(15,2) DEFAULT 0 COMMENT '已开票金额',
    status TINYINT DEFAULT 0 COMMENT '状态：0-未开始，1-进行中，2-已完成',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_tenant (tenant_id),
    INDEX idx_order (sales_order_id)
);
```

#### 开票记录表 (invoice_records)
```sql
CREATE TABLE invoice_records (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    invoice_no VARCHAR(50) NOT NULL COMMENT '发票号码',
    invoice_date DATE NOT NULL COMMENT '开票日期',
    sales_order_id BIGINT NOT NULL COMMENT '销售订单 ID',
    items JSON NOT NULL COMMENT '开票明细（包含类别和金额）',
    total_amount DECIMAL(15,2) NOT NULL COMMENT '开票总金额',
    payment_status TINYINT DEFAULT 0 COMMENT '收款状态：0-未收，1-部分，2-已收',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_tenant (tenant_id),
    INDEX idx_order (sales_order_id),
    INDEX idx_date (invoice_date)
);
```

#### 运费单表 (freight_orders)
```sql
CREATE TABLE freight_orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    freight_no VARCHAR(50) UNIQUE NOT NULL COMMENT '运费单号',
    delivery_order_ids JSON COMMENT '关联发货单 ID 列表',
    carrier VARCHAR(100) COMMENT '承运商/车牌号',
    amount DECIMAL(12,2) NOT NULL COMMENT '运费金额',
    billing_method VARCHAR(50) COMMENT '计费方式',
    payment_status TINYINT DEFAULT 0 COMMENT '付款状态：0-未付，1-已付',
    ship_date DATE COMMENT '发货日期',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_tenant (tenant_id),
    INDEX idx_date (ship_date)
);
```

#### 外协记录表 (subcontract_records)
```sql
CREATE TABLE subcontract_records (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL COMMENT '租户 ID',
    sales_order_id BIGINT COMMENT '关联销售订单 ID',
    supplier_name VARCHAR(100) NOT NULL COMMENT '外协供应商',
    contract_amount DECIMAL(15,2) NOT NULL COMMENT '外协合同金额',
    start_date DATE COMMENT '开始日期',
    end_date DATE COMMENT '结束日期',
    payment_nodes JSON COMMENT '付款节点',
    status TINYINT DEFAULT 0 COMMENT '状态：0-进行中，1-已完成',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_tenant (tenant_id),
    INDEX idx_order (sales_order_id)
);
```

### 4.2 数据库扩展性设计

- 所有业务表均包含 `tenant_id` 字段，支持多租户数据隔离
- 关键查询字段建立索引，优化查询性能
- JSON 类型字段用于存储动态配置和扩展属性
- 时间字段统一使用 DATETIME，便于时区转换

---

## 5. API 接口规范

### 5.1 通用规范

#### 请求格式
- Content-Type: application/json
- 认证方式：Bearer Token (JWT)
- 租户标识：通过 Token 中的 claims 传递

#### 响应格式
```json
{
    "code": 200,
    "message": "success",
    "data": {},
    "timestamp": 1699999999999
}
```

#### 错误码规范
| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 5.2 认证流程

```
1. 用户登录 → POST /api/v1/auth/login
2. 返回 JWT Token（包含 tenant_id, user_id, roles）
3. 后续请求在 Header 中携带：Authorization: Bearer {token}
4. 网关层解析 Token，验证有效性，提取租户信息
5. 路由到对应租户的 Schema/DB
```

---

## 6. 开发计划

### 6.1 迭代规划

#### 第一阶段（P0 - 核心功能）- 4 周
- 用户与权限中心
- 销售与订单管理
- 生产进度管理（基础）
- 智能仓储管理（基础）

#### 第二阶段（P1 - 增强功能）- 3 周
- 发货与客户分享
- 财务管理
- BI 报表中心（基础看板）
- 生产进度管理（高级功能）

#### 第三阶段（P2 - 完善功能）- 3 周
- 发票管理
- 物流费用管理
- 外协管理
- BI 报表中心（高级分析）

### 6.2 目录结构

```
/workspace
├── docs/                    # 文档
│   ├── api/                 # API 文档
│   ├── database/            # 数据库设计文档
│   └── deployment/          # 部署文档
├── backend/                 # 后端代码
│   ├── user-service/        # 用户服务
│   ├── sales-service/       # 销售服务
│   ├── production-service/  # 生产服务
│   ├── warehouse-service/   # 仓储服务
│   ├── delivery-service/    # 发货服务
│   ├── finance-service/     # 财务服务
│   ├── invoice-service/     # 发票服务
│   ├── logistics-service/   # 物流服务
│   ├── subcontract-service/ # 外协服务
│   └── bi-service/          # BI 服务
├── frontend-pc/             # PC 管理后台
│   ├── src/
│   │   ├── views/           # 页面
│   │   ├── components/      # 组件
│   │   ├── api/             # API 调用
│   │   └── store/           # 状态管理
│   └── ...
├── frontend-miniprogram/    # 微信小程序
│   ├── pages/               # 页面
│   ├── components/          # 组件
│   └── utils/               # 工具函数
└── database/                # 数据库脚本
    ├── schema/              # 建表脚本
    ├── data/                # 初始化数据
    └── migration/           # 迁移脚本
```

---

## 附录

### A. 术语表
| 术语 | 说明 |
|------|------|
| MES | 制造执行系统 (Manufacturing Execution System) |
| SaaS | 软件即服务 (Software as a Service) |
| 多租户 | 多个企业共享同一套系统实例，数据隔离 |
| 工艺路线 | 产品加工的工序顺序和标准 |
| 报工 | 工人汇报工作进度和工时 |
| OEE | 设备综合效率 (Overall Equipment Effectiveness) |

### B. 参考文档
- MES 系统设计方案 Saas 版 V2.1.docx
